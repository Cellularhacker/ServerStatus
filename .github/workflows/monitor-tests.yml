name: Monitor tests

on:
  pull_request:
    paths: ['monitoring/**', '.github/workflows/*monitor*.yml']
  push:
    paths: ['monitoring/**', '.github/workflows/*monitor*.yml']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
      - run: python3 -m unittest discover -s monitoring -v
      - run: python3 monitoring/monitor.py --validate
      - run: python3 monitoring/monitor.py --config monitoring/targets.example.json --validate
